{
	"name": "Ruby",
    "build": {
		"dockerfile": "Dockerfile"
	},
	"postCreateCommand": "bundle install && unzip -o gps_images.zip \"gps_images/*\" -d images",
	"customizations": {
		"vscode": {
			"extensions": [
				"Shopify.ruby-lsp"
			]
		}
	}
}
